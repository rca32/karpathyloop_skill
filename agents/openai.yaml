interface:
  display_name: "Bootstrap Skill Autoresearch"
  short_description: "Bootstrap self-improving skill repos with optional scheduled Codex automation support"
  default_prompt: "Use $bootstrap-skill-autoresearch to turn this single-skill Codex repo into an autoresearch scaffold and, after validation, optionally suggest a Codex automation for scheduled runs."
policy:
  allow_implicit_invocation: false
